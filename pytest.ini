[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: tests que entrenan modelos o hacen operaciones costosas (> 10s)
    integration: tests que requieren archivos en disco o red real
addopts = -v --tb=short -p no:warnings